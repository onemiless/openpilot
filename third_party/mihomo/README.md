Bundled mihomo assets for manual GitHub proxy access on C3X devices.

This directory intentionally contains only redistributable runtime files:

- `linux-arm64/mihomo`
- geodata files needed for offline startup

Do not commit subscription URLs, tokens, generated `config.yaml`, logs, or PID files here.
Private runtime state belongs under `/data/mihomo` on the device.
